spring:
  application:
    name: news
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    open-in-view: false
  datasource:
    url: jdbc:postgresql://localhost:5432/news
    username: postgres
    password: Traktaryst
    driver-class-name: org.postgresql.Driver
  liquibase:
    enabled: true
server:
  port: 8080
logging:
  level:
    org:
      springframework:
        transaction: debug
        orm:
          jpa: debug
cache:
  algorithm: LFU
  capacity: 100